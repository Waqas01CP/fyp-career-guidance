import 'package:flutter/material.dart';

/// GradesInputScreen — stub. Implement in Sprint 1/2/3.
/// See CLAUDE.md for screen specifications and Point 5 for API contract.
class GradesInputScreen extends StatelessWidget {
  const GradesInputScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('GradesInputScreen')),
      body: const Center(
        child: Text('TODO: Implement in Sprint'),
      ),
    );
  }
}
