import 'package:flutter/material.dart';

/// AssessmentScreen — stub. Implement in Sprint 1/2/3.
/// See CLAUDE.md for screen specifications and Point 5 for API contract.
class AssessmentScreen extends StatelessWidget {
  const AssessmentScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('AssessmentScreen')),
      body: const Center(
        child: Text('TODO: Implement in Sprint'),
      ),
    );
  }
}
