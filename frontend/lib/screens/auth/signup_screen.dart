import 'package:flutter/material.dart';

/// SignupScreen — stub. Implement in Sprint 1/2/3.
/// See CLAUDE.md for screen specifications and Point 5 for API contract.
class SignupScreen extends StatelessWidget {
  const SignupScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('SignupScreen')),
      body: const Center(
        child: Text('TODO: Implement in Sprint'),
      ),
    );
  }
}
