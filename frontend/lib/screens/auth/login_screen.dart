import 'package:flutter/material.dart';

/// LoginScreen — stub. Implement in Sprint 1/2/3.
/// See CLAUDE.md for screen specifications and Point 5 for API contract.
class LoginScreen extends StatelessWidget {
  const LoginScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('LoginScreen')),
      body: const Center(
        child: Text('TODO: Implement in Sprint'),
      ),
    );
  }
}
