import 'package:flutter/material.dart';

/// RecommendationDashboard — stub. Implement in Sprint 1/2/3.
/// See CLAUDE.md for screen specifications and Point 5 for API contract.
class RecommendationDashboard extends StatelessWidget {
  const RecommendationDashboard({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('RecommendationDashboard')),
      body: const Center(
        child: Text('TODO: Implement in Sprint'),
      ),
    );
  }
}
