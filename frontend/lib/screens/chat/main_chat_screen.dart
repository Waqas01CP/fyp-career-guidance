import 'package:flutter/material.dart';

/// MainChatScreen — stub. Implement in Sprint 1/2/3.
/// See CLAUDE.md for screen specifications and Point 5 for API contract.
class MainChatScreen extends StatelessWidget {
  const MainChatScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('MainChatScreen')),
      body: const Center(
        child: Text('TODO: Implement in Sprint'),
      ),
    );
  }
}
