import 'package:flutter/material.dart';

/// LagScoreBadge — stub. Implement in Sprint 2/3.
/// See CLAUDE.md and Point 5 for specifications.
class LagScoreBadge extends StatelessWidget {
  const LagScoreBadge({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink(); // Replace with real implementation
  }
}
