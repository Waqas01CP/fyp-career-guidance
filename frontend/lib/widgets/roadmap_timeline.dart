import 'package:flutter/material.dart';

/// RoadmapTimeline — stub. Implement in Sprint 2/3.
/// See CLAUDE.md and Point 5 for specifications.
class RoadmapTimeline extends StatelessWidget {
  const RoadmapTimeline({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink(); // Replace with real implementation
  }
}
